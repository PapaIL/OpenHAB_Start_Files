// Node90VibVal  Switch Node90VibState

rule Vibr_Status
  when Item Node90VibVal changed
  then
	var Number V_Stat = Node90VibVal.state

	if(V_Stat < 0) {
		Node90VibState.postUpdate(OFF)
	   }
	if(V_Stat > 0) {
		Node90VibState.postUpdate(ON)
	   }
end